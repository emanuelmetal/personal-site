---
phase: 07-portfolio-showcase
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/app/[lang]/page.tsx
autonomous: false
requirements: [PORT-01, PORT-02, PORT-03]

must_haves:
  truths:
    - "Portfolio section displays 4 project cards in 2-column grid on desktop, 1-column on mobile"
    - "Each card shows project title, description, tech badges, and available action buttons"
    - "External links open in new tabs with security attributes"
    - "Layout adapts correctly at 320px (mobile), 768px (tablet), 1440px (desktop) breakpoints"
    - "Dark mode renders cards with proper contrast (WCAG AA compliant)"
  artifacts:
    - path: "src/app/[lang]/page.tsx"
      provides: "Portfolio section integrated with ProjectGrid component"
      contains: "import ProjectGrid"
      pattern: "<ProjectGrid />"
  key_links:
    - from: "src/app/[lang]/page.tsx"
      to: "src/components/Portfolio/ProjectGrid.tsx"
      via: "import { ProjectGrid } from '@/components/Portfolio'"
      pattern: "import.*ProjectGrid.*@/components/Portfolio"
---

<objective>
Integrate ProjectGrid component into Portfolio section of main page, replacing placeholder content, and verify responsive layout, dark mode styling, and bilingual rendering through manual testing at multiple breakpoints.

Purpose: Complete the Portfolio showcase feature by wiring components to the page and validating the implementation meets success criteria across devices, themes, and languages.

Output: Fully functional Portfolio section displaying 4 professional projects with responsive grid, shadow cards, tech badges, and working external links in both English and Spanish with light/dark mode support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-portfolio-showcase/07-CONTEXT.md
@.planning/phases/07-portfolio-showcase/07-UI-SPEC.md
@.planning/phases/07-portfolio-showcase/07-VALIDATION.md
@.planning/phases/07-portfolio-showcase/07-01-PLAN.md

# Current page structure
@src/app/[lang]/page.tsx

<interfaces>
<!-- Portfolio section placeholder from page.tsx (lines 100-107) -->
From src/app/[lang]/page.tsx:
```typescript
<Section id="portfolio" alternate>
  <h2 className="mb-8 text-xl font-bold text-gray-900 sm:text-2xl lg:text-3xl dark:text-white">
    {t('sections.portfolio')}
  </h2>
  <p className="text-gray-600 dark:text-gray-400">
    {t('placeholders.comingSoon')}
  </p>
</Section>
```

<!-- ProjectGrid component from Plan 01 -->
From src/components/Portfolio/ProjectGrid.tsx:
```typescript
export default function ProjectGrid() {
  const t = useTranslations('portfolio');
  const projects = t.raw('projects') as Project[];

  return (
    <div className="grid grid-cols-1 gap-6 lg:grid-cols-2 lg:gap-8">
      {projects.map((project, index) => (
        <ProjectCard key={`${project.name}-${index}`} project={project} />
      ))}
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Portfolio section placeholder with ProjectGrid component</name>
  <files>src/app/[lang]/page.tsx</files>
  <action>
Import ProjectGrid component and replace Portfolio section placeholder content.

**At top of file (after existing imports):**
Add import statement:
```typescript
import { ProjectGrid } from '@/components/Portfolio';
```

**In Portfolio section (lines 100-107):**
Replace the placeholder paragraph:
```typescript
<p className="text-gray-600 dark:text-gray-400">
  {t('placeholders.comingSoon')}
</p>
```

With ProjectGrid component:
```typescript
<ProjectGrid />
```

**Complete Portfolio section structure after change:**
```typescript
<Section id="portfolio" alternate>
  <h2 className="mb-8 text-xl font-bold text-gray-900 sm:text-2xl lg:text-3xl dark:text-white">
    {t('sections.portfolio')}
  </h2>
  <ProjectGrid />
</Section>
```

Preserve the existing Section wrapper with `id="portfolio"` and `alternate` prop (gray background). Keep section heading unchanged.
  </action>
  <verify>
    <automated>npm run build && grep -q "import.*ProjectGrid" src/app/[lang]/page.tsx && echo "✓ ProjectGrid imported and build passed"</automated>
  </verify>
  <done>page.tsx imports ProjectGrid from @/components/Portfolio, Portfolio section renders ProjectGrid instead of placeholder, build succeeds without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Portfolio section responsive layout, dark mode, and bilingual rendering</name>
  <what-built>
Portfolio section with 4 project cards displaying in responsive 2-column grid with shadow styling, tech badges, and conditional action buttons for demo and code links.
  </what-built>
  <how-to-verify>
Start dev server and test Portfolio section across breakpoints, themes, and languages:

**1. Desktop Layout Verification (≥1024px):**
```bash
npm run dev
```
- Open http://localhost:3000/en in browser
- Scroll to Portfolio section
- ✅ Verify 4 project cards appear in 2×2 grid layout
- ✅ Verify cards have white background with shadow in light mode
- ✅ Verify each card shows: title (large, bold), description (gray text), tech badges (neutral gray pills), action buttons
- ✅ Verify Project 1 (Agoda) shows 2 "View Project" buttons (demoUrl + alternateUrl)
- ✅ Verify Project 2 (Rocket Travel) shows 2 "View Project" buttons
- ✅ Verify Project 3 (N95) shows 1 "View Project" button
- ✅ Verify Project 4 (Alix Partners) shows 1 "View Code" button (gray border, GitHub link)
- ✅ Verify cards have rounded corners and shadow increases on hover

**2. Mobile Layout Verification (320px):**
- Open browser DevTools, set viewport to 320px width
- ✅ Verify cards stack in single column (no horizontal scroll)
- ✅ Verify card content remains readable (no text cutoff)
- ✅ Verify buttons may wrap to 2 rows if both demo and code links present
- ✅ Verify 24px gap between stacked cards (gap-6)

**3. Tablet Layout Verification (768px):**
- Set viewport to 768px width
- ✅ Verify layout behavior (1-column or transition to 2-column at lg breakpoint)
- ✅ Verify card spacing and readability

**4. Dark Mode Verification:**
- Click theme toggle to switch to dark mode
- ✅ Verify cards have gray-800 background (darker than Portfolio section gray-900)
- ✅ Verify card shadows still visible against dark background
- ✅ Verify title text is white (high contrast)
- ✅ Verify description text is gray-300 (readable contrast)
- ✅ Verify tech badges use gray-700 background with gray-300 text
- ✅ Verify "View Project" buttons have blue-400 border/text
- ✅ Verify "View Code" buttons have gray-400 border/text
- ✅ Verify hover states work (buttons show subtle background color on hover)

**5. External Link Verification:**
- Right-click "View Project" button on Agoda card → "Open Link in New Tab"
- ✅ Verify link opens https://www.anaworldhotel.com/ in new tab
- Click second "View Project" button on Agoda card
- ✅ Verify opens https://www.citi.com/citi-partner/cititravel/login in new tab
- Click "View Code" button on Alix Partners card
- ✅ Verify opens https://github.com/emanuelmetal/alix-survey-project in new tab
- Inspect button element in DevTools
- ✅ Verify has `target="_blank"` and `rel="noopener noreferrer"` attributes
- ✅ Verify has `aria-label` attribute with descriptive text

**6. Bilingual Verification:**
- Switch to Spanish language (/es)
- ✅ Verify section heading displays "Portfolio"
- ✅ Verify all 4 project descriptions display in Spanish
- ✅ Verify button labels show "Ver Proyecto" and "Ver Código"
- ✅ Verify project names remain in English (proper nouns)
- ✅ Verify tech stack names remain in English (universal)
- ✅ Verify URLs are identical to English version

**7. Accessibility Verification:**
- Use keyboard only (Tab key) to navigate
- ✅ Verify all action buttons are reachable via Tab
- ✅ Verify focus indicator visible on buttons
- ✅ Verify Enter key opens links
- Open browser DevTools → Accessibility panel
- ✅ Verify contrast ratios meet WCAG AA (4.5:1 for text)
- ✅ Verify no accessibility warnings in console

**Expected Behavior:**
- All 4 projects render in Portfolio section
- Grid layout adapts: 2-column on desktop, 1-column on mobile
- Cards have shadow styling with rounded corners
- Tech badges use neutral gray (different from Skills section colors)
- Action buttons show conditionally (projects 1-3 have demo links, project 4 has GitHub)
- External links open in new tabs securely
- Dark mode and bilingual rendering work correctly
- No visual glitches, broken layouts, or missing content
  </how-to-verify>
  <resume-signal>
Type one of:
- "approved" - All verifications passed, Portfolio section works correctly
- "issue: [description]" - Describe specific problem found (broken layout, missing links, accessibility violation, etc.)
  </resume-signal>
</task>

</tasks>

<verification>
**Final phase verification checklist:**

**Success Criteria from ROADMAP.md:**
1. ✅ User can view showcase of 4 professional projects in Portfolio section
2. ✅ Each project card displays title, description, and technologies used
3. ✅ User can click project links to access live demos or repositories where available
4. ✅ Project cards are visually consistent and professionally styled
5. ✅ Portfolio section works on mobile and desktop with responsive grid layout

**Requirements Validated:**
- **PORT-01**: User can view showcase of professional projects → 4 projects visible in grid
- **PORT-02**: Each project displays title, description, and technologies used → All cards show complete data with tech badges
- **PORT-03**: User can access project links → Demo and code buttons work, open in new tabs

**Technical Compliance:**
- TypeScript compilation passes without errors
- No console warnings or errors in browser
- WCAG AA contrast ratios met in light and dark modes
- External links have proper security attributes (target="_blank" rel="noopener noreferrer")
- Responsive layout works at 320px, 768px, and 1440px breakpoints
- Bilingual content renders correctly in English and Spanish
</verification>

<success_criteria>
**Plan succeeds when:**

1. ✅ Portfolio section in page.tsx imports and renders ProjectGrid component
2. ✅ 4 project cards display in responsive grid (2-column desktop, 1-column mobile)
3. ✅ Each card shows: title (h3), description (p), tech badges (neutral gray), action buttons (conditional)
4. ✅ Project 1 and 2 (Agoda, Rocket Travel) show 2 demo buttons each (demoUrl + alternateUrl)
5. ✅ Project 3 (N95) shows 1 demo button
6. ✅ Project 4 (Alix Partners) shows 1 code button (GitHub)
7. ✅ All external links open in new tabs with security attributes and aria-labels
8. ✅ Dark mode renders cards with gray-800 background and proper text contrast
9. ✅ Spanish language displays translated descriptions and button labels
10. ✅ Responsive layout verified at mobile (320px), tablet (768px), and desktop (1440px) breakpoints
11. ✅ No accessibility violations (keyboard navigation, contrast ratios, focus indicators)
12. ✅ Build passes and dev server runs without errors

**Phase 7 complete when checkpoint approved.**
</success_criteria>

<output>
After checkpoint approval, create `.planning/phases/07-portfolio-showcase/07-02-SUMMARY.md` documenting:
- Page integration completed (ProjectGrid replacing placeholder)
- Responsive layout verified across breakpoints
- Dark mode styling validated
- Bilingual rendering confirmed (English/Spanish)
- External link functionality tested
- Accessibility compliance verified
- Screenshots or descriptions of any visual refinements made
- Any issues discovered and resolved during verification
- Recommendations for Phase 8 (Performance) if applicable
</output>
