---
phase: 05-core-content-sections
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/app/[lang]/page.tsx
autonomous: false
requirements:
  - ABOUT-01
  - ABOUT-02
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "User can read a 2-3 sentence professional bio next to a profile photo in a two-column layout"
    - "User can view skills organized by 4 categories with 3 experience tiers displayed as colored badges"
    - "User can find contact email as a mailto link"
    - "User can click LinkedIn and GitHub icon buttons that open in new tabs"
    - "All content renders in both English and Spanish"
    - "Layout stacks vertically on mobile and displays side-by-side on desktop"
  artifacts:
    - path: "src/app/[lang]/page.tsx"
      provides: "About two-column layout, Skills badges, Contact links"
      min_lines: 80
      contains: "grid-cols-1 lg:grid-cols-2"
  key_links:
    - from: "src/app/[lang]/page.tsx"
      to: "messages/en.json"
      via: "useTranslations hook"
      pattern: "t\\('about\\."
    - from: "src/app/[lang]/page.tsx"
      to: "messages/en.json"
      via: "useTranslations hook"
      pattern: "t\\('skills\\."
    - from: "src/app/[lang]/page.tsx"
      to: "messages/en.json"
      via: "useTranslations hook"
      pattern: "t\\('contact\\."
    - from: "src/app/[lang]/page.tsx"
      to: "lucide-react"
      via: "import"
      pattern: "import.*Linkedin.*Github.*from 'lucide-react'"
---

<objective>
Implement About (two-column), Skills (categorized badges), and Contact (email + social links) sections in page.tsx using content from translation files.

Purpose: Transforms placeholder sections into real professional content that recruiters can read and interact with. This is the core user-facing deliverable of Phase 5.
Output: Fully rendered About, Skills, and Contact sections with responsive layout, dark mode support, and bilingual content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-core-content-sections/05-01-SUMMARY.md
@src/app/[lang]/page.tsx
@src/components/Section.tsx
@messages/en.json

<interfaces>
<!-- From src/components/Section.tsx -->
```typescript
type SectionProps = {
  id: string;
  children: React.ReactNode;
  alternate?: boolean;
};
export default function Section({ id, children, alternate = false }: SectionProps)
```

<!-- Section provides: px-4 py-12 sm:px-8 lg:px-12 lg:py-16, mx-auto max-w-7xl container -->
<!-- alternate=true: bg-gray-50 dark:bg-gray-900, alternate=false: bg-white dark:bg-gray-950 -->

<!-- From next-intl (used in page.tsx) -->
```typescript
import { useTranslations } from 'next-intl';
const t = useTranslations();
// Access: t('about.bio'), t('skills.categories.frontend'), t('contact.cta')
// For dynamic keys with TypeScript: use type assertion (t(`key.${var}` as any))
```

<!-- From lucide-react (already installed, v0.577.0) -->
```typescript
import { Linkedin, Github, Mail } from 'lucide-react';
// Usage: <Linkedin className="h-5 w-5" />
```

<!-- Translation keys available after Plan 01 -->
- t('about.bio') — 2-3 sentence professional summary
- t('about.description') — extended professional summary
- t('about.heading') — "About Me" / "Sobre Mi"
- t('skills.heading') — "Skills & Technologies"
- t('skills.categories.frontend') — "Frontend"
- t('skills.categories.backend') — "Backend"
- t('skills.categories.cloud') — "Cloud & DevOps"
- t('skills.categories.tools') — "Tools & Practices"
- t('skills.tiers.10plus') — "10+ years" / "10+ anios"
- t('skills.tiers.5to10') — "5-10 years" / "5-10 anios"
- t('skills.tiers.2to5') — "2-5 years" / "2-5 anios"
- skills.data.{category}.{tier} — arrays of skill name strings (accessed via raw JSON import or t() with type assertion)
- t('contact.heading') — "Get In Touch"
- t('contact.cta') — CTA message
- t('contact.email') — "Email" label
- t('contact.emailAddress') — actual email address
- t('contact.linkedin') — "LinkedIn" label
- t('contact.linkedinUrl') — actual LinkedIn URL
- t('contact.github') — "GitHub" label
- t('contact.githubUrl') — actual GitHub URL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement About, Skills, and Contact sections in page.tsx</name>
  <files>src/app/[lang]/page.tsx</files>
  <read_first>
    - src/app/[lang]/page.tsx (current layout to restructure)
    - src/components/Section.tsx (Section component interface)
    - messages/en.json (translation keys available — especially skills.data structure)
    - .planning/phases/05-core-content-sections/05-CONTEXT.md (decisions D-03 through D-14)
    - .planning/phases/05-core-content-sections/05-RESEARCH.md (code examples and anti-patterns)
  </read_first>
  <action>
Rewrite src/app/[lang]/page.tsx to implement all three content sections. Keep existing imports and add lucide-react import. Preserve the experience and portfolio sections as-is (still placeholder).

**Read node_modules/next/dist/docs/ first** per CLAUDE.md directive — verify next/image and useTranslations patterns are current for Next.js 16.

**Imports to add at top of file:**
```tsx
import { Linkedin, Github, Mail } from 'lucide-react';
```

**About section** (per D-03, D-04, D-05, D-06):
Replace the current center-aligned `<Section id="about">` with a two-column grid layout:
- Outer: `<div className="grid grid-cols-1 gap-8 lg:grid-cols-2 lg:gap-12">`
- Left column: Profile photo using `<Image src="/placeholder.svg" alt="Emanuel Pereyra" width={200} height={200} priority={true} className="rounded-full" />` wrapped in `<div className="flex justify-center lg:justify-start">`
- Right column: `<div className="flex flex-col justify-center">` containing:
  - `<h1>` with text "Emanuel Pereyra" using classes `mb-2 text-2xl font-bold tracking-tight text-gray-900 sm:text-3xl lg:text-4xl dark:text-white`
  - `<p>` with `{t('home.heading')}` using classes `mb-4 text-lg font-medium text-blue-600 dark:text-blue-400` (role/title)
  - `<p>` with `{t('about.bio')}` using classes `text-lg text-gray-600 sm:text-xl dark:text-gray-300`
  - `<p>` with `{t('about.description')}` using classes `mt-2 text-base text-gray-500 sm:text-lg dark:text-gray-400`

**Skills section** (per D-07, D-08, D-09, D-10):
Replace placeholder in `<Section id="skills">` with categorized badge display:

Define category colors as a constant object INSIDE the component (before JSX return):
```tsx
const categoryColors: Record<string, string> = {
  frontend: 'bg-blue-100 text-blue-800 dark:bg-blue-900/50 dark:text-blue-300',
  backend: 'bg-green-100 text-green-800 dark:bg-green-900/50 dark:text-green-300',
  cloud: 'bg-purple-100 text-purple-800 dark:bg-purple-900/50 dark:text-purple-300',
  tools: 'bg-orange-100 text-orange-800 dark:bg-orange-900/50 dark:text-orange-300',
};
const categories = ['frontend', 'backend', 'cloud', 'tools'] as const;
const tiers = ['10plus', '5to10', '2to5'] as const;
```

Structure inside the Section:
- `<h2>` heading with `{t('skills.heading')}` using same heading classes as existing sections
- `<div className="space-y-10">` wrapping all categories
- For each category: `<div key={category}>` with:
  - `<h3 className="mb-4 text-lg font-semibold text-gray-900 dark:text-white">{t(`skills.categories.${category}` as any)}</h3>`
  - `<div className="space-y-3">` wrapping all tiers within category
  - For each tier: `<div key={tier}>` with:
    - `<p className="mb-2 text-sm font-medium text-gray-500 dark:text-gray-400">{t(`skills.tiers.${tier}` as any)}</p>`
    - `<div className="flex flex-wrap gap-2">` containing skill badges
    - For skill items: Access skills.data via raw message import OR use `t.raw(`skills.data.${category}.${tier}`)` to get the array. Map over it rendering:
      ```tsx
      <span key={skill} className={`inline-block rounded-full px-3 py-1.5 text-sm font-medium transition-transform hover:scale-105 ${categoryColors[category]}`}>
        {skill}
      </span>
      ```

**IMPORTANT for skills data access:** next-intl's `t()` returns strings, not arrays. To access the skills arrays, use one of:
1. `t.raw('skills.data.frontend.10plus')` — returns the raw JSON value (array)
2. Import messages JSON directly and access `messages.skills.data[category][tier]`
Check next-intl docs for `t.raw()` availability in v4.8.3. If not available, import JSON directly.

**Contact section** (per D-11, D-12, D-13, D-14):
Replace placeholder in `<Section id="contact">` with centered layout:
- `<div className="mx-auto max-w-2xl text-center">`
- `<h2>` heading with `{t('contact.heading')}` using same heading classes
- `<p className="mb-8 text-lg text-gray-600 dark:text-gray-300">{t('contact.cta')}</p>` (CTA per D-12)
- Email link (per D-11, CONT-01):
  ```tsx
  <div className="mb-6">
    <a
      href={`mailto:${t('contact.emailAddress')}`}
      className="inline-flex items-center gap-2 text-lg font-medium text-blue-600 underline transition-colors hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300"
    >
      <Mail className="h-5 w-5" />
      <span>{t('contact.emailAddress')}</span>
    </a>
  </div>
  ```
- Social links row (per D-13, D-14, CONT-02, CONT-03, CONT-04):
  ```tsx
  <div className="flex justify-center gap-4">
    <a
      href={t('contact.linkedinUrl')}
      target="_blank"
      rel="noopener noreferrer"
      className="flex items-center gap-2 rounded-lg px-4 py-2 text-gray-700 transition-colors hover:bg-gray-100 hover:text-gray-900 dark:text-gray-300 dark:hover:bg-gray-800 dark:hover:text-white"
      aria-label="LinkedIn profile"
    >
      <Linkedin className="h-5 w-5" />
      <span className="text-sm font-medium">{t('contact.linkedin')}</span>
    </a>
    <a
      href={t('contact.githubUrl')}
      target="_blank"
      rel="noopener noreferrer"
      className="flex items-center gap-2 rounded-lg px-4 py-2 text-gray-700 transition-colors hover:bg-gray-100 hover:text-gray-900 dark:text-gray-300 dark:hover:bg-gray-800 dark:hover:text-white"
      aria-label="GitHub profile"
    >
      <Github className="h-5 w-5" />
      <span className="text-sm font-medium">{t('contact.github')}</span>
    </a>
  </div>
  ```

**Preserve unchanged sections:**
- `<Section id="experience" alternate>` — keep as-is with placeholder
- `<Section id="portfolio" alternate>` — keep as-is with placeholder

**Order of sections in JSX (unchanged):** about, experience, skills, portfolio, contact
  </action>
  <verify>
    <automated>cd /Users/emanuelpereyra/gitRepos/own/personal-site && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contains `import { Linkedin, Github, Mail } from 'lucide-react'`
    - page.tsx contains `grid grid-cols-1` and `lg:grid-cols-2` (About two-column layout per D-03)
    - page.tsx contains `t('about.bio')` (bio from translations per D-04)
    - page.tsx contains `rounded-full` for badge styling (per D-09)
    - page.tsx contains `categoryColors` object with keys frontend, backend, cloud, tools
    - page.tsx contains `t('skills.categories.` (category titles from translations per D-07)
    - page.tsx contains `t('skills.tiers.` (tier labels from translations per D-08)
    - page.tsx contains `mailto:` (email link per CONT-01)
    - page.tsx contains `target="_blank"` at least twice (LinkedIn and GitHub per CONT-04)
    - page.tsx contains `rel="noopener noreferrer"` at least twice (security per D-14)
    - page.tsx contains `aria-label` for LinkedIn and GitHub links (accessibility per D-13)
    - page.tsx contains `t('contact.linkedinUrl')` and `t('contact.githubUrl')` (URLs from translations)
    - page.tsx still contains `<Section id="experience"` and `<Section id="portfolio"` (preserved)
    - page.tsx still contains `placeholders.comingSoon` (experience/portfolio unchanged)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>About section displays two-column layout with photo and bio, Skills section shows 4 categories with 3 tiers of colored badges, Contact section has email mailto link and LinkedIn/GitHub icon buttons opening in new tabs. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all three sections</name>
  <files>src/app/[lang]/page.tsx</files>
  <action>
Human visually verifies About, Skills, and Contact sections in browser.

What was built: About (two-column with photo and bio), Skills (4 categories x 3 tiers with colored badges), and Contact (email and LinkedIn/GitHub icon buttons) sections with responsive layout and dark mode support.

How to verify:
1. Run `npm run dev` and open http://localhost:3000/en
2. About section: Verify two-column layout — photo on left, name + bio on right (desktop). Test at 320px viewport — should stack vertically.
3. Skills section: Verify 4 category headings (Frontend, Backend, Cloud and DevOps, Tools and Practices). Under each category, verify 3 tier labels (10+ years, 5-10 years, 2-5 years). Verify skills appear as colored rounded badges (blue=Frontend, green=Backend, purple=Cloud, orange=Tools).
4. Contact section: Verify CTA text visible. Verify email link opens mail client. Verify LinkedIn and GitHub buttons have icons AND text labels. Click each — should open new tab.
5. Dark mode: Toggle theme to dark. Verify all sections readable, badges have adequate contrast, links visible.
6. Spanish: Switch to http://localhost:3000/es — verify all content translated (bio in Spanish, tier labels "10+ anios", CTA in Spanish).
7. Responsive: Test at 320px width — all sections should stack vertically, no horizontal scrollbar.

Resume signal: Type "approved" or describe specific issues to fix.
  </action>
  <verify>
    <automated>cd /Users/emanuelpereyra/gitRepos/own/personal-site && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms all three sections render correctly in light/dark mode, both languages, and responsive breakpoints</done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- About section has responsive two-column grid (grid-cols-1 lg:grid-cols-2)
- Skills section renders 4 categories x 3 tiers with badge styling
- Contact section has mailto link, LinkedIn button, GitHub button all with proper attributes
- All social links have target="_blank" and rel="noopener noreferrer"
- Content renders in both EN and ES via translation files
- Dark mode styling applied to all sections
</verification>

<success_criteria>
- User can read professional bio next to profile photo in two-column layout (ABOUT-01, ABOUT-02)
- User can view 4 skill categories with 3 experience tiers each as colored badges (SKILL-01, SKILL-02, SKILL-03)
- User can click email mailto link (CONT-01)
- User can click LinkedIn icon button opening in new tab (CONT-02, CONT-04)
- User can click GitHub icon button opening in new tab (CONT-03, CONT-04)
- All content bilingual (EN/ES)
- Layout responsive: stacks on mobile, side-by-side on desktop
- Dark mode works for all sections
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-content-sections/05-02-SUMMARY.md`
</output>
