---
phase: 05-core-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - messages/es.json
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "Translation files contain real professional bio text in EN and ES"
    - "Skills data has 4 categories (frontend, backend, cloud, tools) each with 3 tiers (10plus, 5to10, 2to5)"
    - "Contact data includes email address, LinkedIn URL, and GitHub URL"
    - "EN and ES translation files have parallel structure with identical keys"
  artifacts:
    - path: "messages/en.json"
      provides: "English translations with bio, skills data, contact info"
      contains: "skills.data.frontend.10plus"
    - path: "messages/es.json"
      provides: "Spanish translations with bio, skills data, contact info"
      contains: "skills.data.frontend.10plus"
  key_links:
    - from: "messages/en.json"
      to: "messages/es.json"
      via: "parallel key structure"
      pattern: "skills\\.data\\."
---

<objective>
Populate translation files (EN/ES) with real professional content for About, Skills, and Contact sections.

Purpose: Establishes all bilingual content data that the UI components (Plan 02) will consume. Content-first approach ensures translation structure is validated before building the rendering layer.
Output: Complete en.json and es.json with bio, categorized skills (4 categories x 3 tiers), and contact information including social URLs (per CONT-04, links open in new tabs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@messages/en.json
@messages/es.json

<interfaces>
<!-- Current translation file structure (from messages/en.json) -->
Existing keys that MUST be preserved:
- metadata.title, metadata.description
- home.heading, home.subheading
- nav.about, nav.experience, nav.skills, nav.portfolio, nav.contact
- theme.toggleLabel, theme.cycleHint, theme.light, theme.dark, theme.system
- sections.experience, sections.skills, sections.portfolio, sections.contact
- placeholders.comingSoon
- about.heading, about.bio, about.description
- experience.heading, experience.timeline.present
- skills.heading, skills.categories.frontend, skills.categories.backend, skills.categories.cloud, skills.categories.tools
- portfolio.heading, portfolio.viewProject, portfolio.viewCode
- contact.heading, contact.email, contact.linkedin, contact.github, contact.cta
- language.switchToEnglish, language.switchToSpanish

New keys to ADD (do not remove existing):
- about.bio (UPDATE with richer 2-3 sentence bio)
- about.description (UPDATE with expanded professional summary)
- skills.tiers.10plus, skills.tiers.5to10, skills.tiers.2to5 (tier labels)
- skills.data.frontend.10plus[], skills.data.frontend.5to10[], skills.data.frontend.2to5[]
- skills.data.backend.10plus[], skills.data.backend.5to10[], skills.data.backend.2to5[]
- skills.data.cloud.10plus[], skills.data.cloud.5to10[], skills.data.cloud.2to5[]
- skills.data.tools.10plus[], skills.data.tools.5to10[], skills.data.tools.2to5[]
- contact.emailAddress (actual email address value)
- contact.linkedinUrl (actual LinkedIn URL — per CONT-02, CONT-04)
- contact.githubUrl (actual GitHub URL — per CONT-03, CONT-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update English translation file with real content</name>
  <files>messages/en.json</files>
  <read_first>
    - messages/en.json (current structure to preserve)
    - .planning/phases/05-core-content-sections/05-CONTEXT.md (decisions D-01 through D-14)
    - .planning/phases/05-core-content-sections/05-RESEARCH.md (translation file structure pattern)
  </read_first>
  <action>
Update messages/en.json preserving ALL existing keys. Add/update these specific sections:

**about section** (per D-01, D-04, D-05 -- real content, brief bio, just photo + bio):
- Update `about.bio` to a 2-3 sentence professional summary: "Senior Software Engineer with over 19 years of experience designing and building scalable, high-quality software solutions. Specialized in full-stack web development with deep expertise in JavaScript/TypeScript ecosystems, cloud infrastructure, and engineering best practices."
- Update `about.description` to: "Passionate about clean architecture, performance optimization, and mentoring engineering teams. Currently focused on modern React/Next.js applications and cloud-native solutions."

**skills section** (per D-07, D-08, D-10 -- 4 categories, tier-based grouping, categories first with tiers nested):
Add `skills.tiers` object with tier display labels:
```json
"tiers": {
  "10plus": "10+ years",
  "5to10": "5-10 years",
  "2to5": "2-5 years"
}
```

Add `skills.data` object with this exact structure (skills are NOT translated -- tech names are universal):
```json
"data": {
  "frontend": {
    "10plus": ["JavaScript", "TypeScript", "React", "HTML/CSS", "Responsive Design"],
    "5to10": ["Next.js", "Vue.js", "Tailwind CSS", "Webpack"],
    "2to5": ["React Server Components", "Astro", "Vite"]
  },
  "backend": {
    "10plus": ["Node.js", "Express", "REST APIs", "PostgreSQL"],
    "5to10": ["GraphQL", "MongoDB", "Redis", "Microservices"],
    "2to5": ["Prisma", "tRPC", "Serverless Functions"]
  },
  "cloud": {
    "10plus": ["Git", "CI/CD", "Linux", "Docker"],
    "5to10": ["AWS", "Kubernetes", "Terraform", "GitHub Actions"],
    "2to5": ["Vercel", "Railway", "Cloudflare Workers"]
  },
  "tools": {
    "10plus": ["Agile/Scrum", "Code Review", "Testing Strategies", "Technical Documentation"],
    "5to10": ["Jest", "ESLint", "Storybook", "Monorepos"],
    "2to5": ["Playwright", "Vitest", "Turborepo"]
  }
}
```

**contact section** (per D-11, D-12, D-13 -- email, LinkedIn, GitHub with URLs; per CONT-04 -- links open in new tabs, URLs must be present for Plan 02 to wire target="_blank"):
Add these keys alongside existing contact keys:
```json
"emailAddress": "contact@emanuelpereyra.com",
"linkedinUrl": "https://www.linkedin.com/in/emanuelpereyra",
"githubUrl": "https://github.com/emanuelmetal"
```

The GitHub username `emanuelmetal` is derived from the git remote origin. LinkedIn URL and email use reasonable defaults -- user can update before deployment.

Do NOT remove any existing keys. The file must remain valid JSON.
  </action>
  <verify>
    <automated>cd /Users/emanuelpereyra/gitRepos/own/personal-site && node -e "const j=require('./messages/en.json'); const checks = [j.skills.data.frontend['10plus'].length >= 4, j.skills.data.backend['10plus'].length >= 3, j.skills.data.cloud['10plus'].length >= 3, j.skills.data.tools['10plus'].length >= 3, j.skills.tiers['10plus'] === '10+ years', j.contact.emailAddress.includes('@'), j.contact.linkedinUrl.includes('linkedin.com'), j.contact.githubUrl.includes('github.com'), j.about.bio.length > 80, j.nav.about === 'About']; console.log(checks.every(Boolean) ? 'PASS' : 'FAIL: ' + checks.join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - messages/en.json is valid JSON (node -e "require('./messages/en.json')" exits 0)
    - en.json contains key path skills.data.frontend.10plus as an array with >= 4 items
    - en.json contains key path skills.data.backend.5to10 as an array with >= 3 items
    - en.json contains key path skills.tiers.10plus with value "10+ years"
    - en.json contains key path skills.tiers.5to10 with value "5-10 years"
    - en.json contains key path skills.tiers.2to5 with value "2-5 years"
    - en.json contains key path contact.emailAddress with value containing "@"
    - en.json contains key path contact.linkedinUrl with value containing "linkedin.com"
    - en.json contains key path contact.githubUrl with value containing "github.com"
    - en.json about.bio is at least 80 characters (real content, not placeholder)
    - All existing keys preserved: nav.about === "About", theme.toggleLabel exists, placeholders.comingSoon exists
  </acceptance_criteria>
  <done>English translation file contains complete real content for About bio, 4 skill categories with 3 tiers each, and contact information with email/LinkedIn/GitHub URLs</done>
</task>

<task type="auto">
  <name>Task 2: Update Spanish translation file with parallel content</name>
  <files>messages/es.json</files>
  <read_first>
    - messages/es.json (current structure to preserve)
    - messages/en.json (updated English file -- must match structure exactly)
  </read_first>
  <action>
Update messages/es.json preserving ALL existing keys, mirroring the exact same structure as updated en.json.

**about section** (Spanish translations of the English bio):
- Update `about.bio` to: "Ingeniero de Software Senior con mas de 19 anios de experiencia disenando y construyendo soluciones de software escalables y de alta calidad. Especializado en desarrollo web full-stack con profunda experiencia en ecosistemas JavaScript/TypeScript, infraestructura en la nube y mejores practicas de ingenieria."
- Update `about.description` to: "Apasionado por la arquitectura limpia, la optimizacion de rendimiento y la mentoria de equipos de ingenieria. Actualmente enfocado en aplicaciones modernas con React/Next.js y soluciones nativas de la nube."

**skills section** -- Add `skills.tiers` with Spanish labels:
```json
"tiers": {
  "10plus": "10+ anios",
  "5to10": "5-10 anios",
  "2to5": "2-5 anios"
}
```

Add `skills.data` -- IDENTICAL to English (tech names are universal, not translated):
```json
"data": {
  "frontend": {
    "10plus": ["JavaScript", "TypeScript", "React", "HTML/CSS", "Responsive Design"],
    "5to10": ["Next.js", "Vue.js", "Tailwind CSS", "Webpack"],
    "2to5": ["React Server Components", "Astro", "Vite"]
  },
  "backend": {
    "10plus": ["Node.js", "Express", "REST APIs", "PostgreSQL"],
    "5to10": ["GraphQL", "MongoDB", "Redis", "Microservices"],
    "2to5": ["Prisma", "tRPC", "Serverless Functions"]
  },
  "cloud": {
    "10plus": ["Git", "CI/CD", "Linux", "Docker"],
    "5to10": ["AWS", "Kubernetes", "Terraform", "GitHub Actions"],
    "2to5": ["Vercel", "Railway", "Cloudflare Workers"]
  },
  "tools": {
    "10plus": ["Agile/Scrum", "Code Review", "Testing Strategies", "Technical Documentation"],
    "5to10": ["Jest", "ESLint", "Storybook", "Monorepos"],
    "2to5": ["Playwright", "Vitest", "Turborepo"]
  }
}
```

**contact section** -- Same URLs (not translated), but label "Correo" already exists:
```json
"emailAddress": "contact@emanuelpereyra.com",
"linkedinUrl": "https://www.linkedin.com/in/emanuelpereyra",
"githubUrl": "https://github.com/emanuelmetal"
```

Do NOT remove any existing keys. File must be valid JSON.
  </action>
  <verify>
    <automated>cd /Users/emanuelpereyra/gitRepos/own/personal-site && node -e "const en=require('./messages/en.json'); const es=require('./messages/es.json'); const checks = [es.skills.data.frontend['10plus'].length === en.skills.data.frontend['10plus'].length, es.skills.tiers['10plus'] === '10+ anios', es.contact.emailAddress === en.contact.emailAddress, es.contact.linkedinUrl === en.contact.linkedinUrl, es.about.bio.length > 80, es.nav.about === 'Acerca']; console.log(checks.every(Boolean) ? 'PASS' : 'FAIL: ' + checks.join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - messages/es.json is valid JSON (node -e "require('./messages/es.json')" exits 0)
    - es.json skills.data structure has identical keys and array lengths as en.json
    - es.json skills.tiers.10plus equals "10+ anios"
    - es.json skills.tiers.5to10 equals "5-10 anios"
    - es.json contact.emailAddress equals en.json contact.emailAddress (same value)
    - es.json contact.linkedinUrl equals en.json contact.linkedinUrl (same value)
    - es.json about.bio is at least 80 characters and contains "19"
    - All existing keys preserved: nav.about === "Acerca", contact.cta contains "equipo"
  </acceptance_criteria>
  <done>Spanish translation file contains complete real content parallel to English, with translated bio/tier labels and identical skills data and contact URLs</done>
</task>

</tasks>

<verification>
- Both translation files are valid JSON: `node -e "require('./messages/en.json'); require('./messages/es.json'); console.log('PASS')"`
- Skills data has 4 categories each with 3 tiers in both files
- Contact URLs are identical between EN and ES files
- All pre-existing translation keys still present in both files
- `npm run build` passes without errors
</verification>

<success_criteria>
- English and Spanish translation files contain real professional content (not placeholders) for About, Skills, and Contact
- Skills organized in 4 categories with 3 experience tiers each (per D-07, D-08, D-10)
- Contact section has email address, LinkedIn URL, and GitHub URL (per CONT-01, CONT-02, CONT-03, CONT-04)
- Both files maintain structural parity
- Build still passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-content-sections/05-01-SUMMARY.md`
</output>
