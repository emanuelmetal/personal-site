---
phase: 09-accessibility-quality
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - tailwind.config.ts
  - src/app/globals.css
  - src/components/Section.tsx
  - src/app/[lang]/page.tsx
  - src/components/Header.tsx
autonomous: true
requirements: [QUAL-01, QUAL-02]

must_haves:
  truths:
    - "Site displays clean, minimal aesthetic with generous whitespace (Stripe/Vercel reference)"
    - "Typography has refined font weights, line heights, and letter spacing for professional polish"
    - "Color palette uses cooler slate tones instead of neutral grays"
    - "Light mode alternate sections use subtle gray-50 background for visual separation"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "Slate color scale configuration"
      contains: "slate"
    - path: "src/components/Section.tsx"
      provides: "Increased padding for whitespace rhythm"
      pattern: "py-\\d+"
    - path: "src/app/[lang]/page.tsx"
      provides: "Refined typography with adjusted weights and spacing"
      pattern: "font-(medium|semibold|bold)"
  key_links:
    - from: "tailwind.config.ts"
      to: "All components using gray-* classes"
      via: "Slate scale replacing neutral gray"
      pattern: "slate"
    - from: "src/components/Section.tsx"
      to: "All page sections"
      via: "Alternate prop controls background color"
      pattern: "alternate.*bg-"
    - from: "src/app/globals.css"
      to: "All text elements"
      via: "Base typography styles and transitions"
      pattern: "font-|line-height|letter-spacing"
---

<objective>
Transform the site's visual design to a modern corporate aesthetic with clean typography, generous whitespace, and a professional color palette.

Purpose: Elevate the design from functional to polished, conveying senior-level professionalism through refined typography, spacing, and color choices. This addresses QUAL-01 (professional aesthetic) and QUAL-02 (readable typography).

Output: Site displays Stripe/Vercel-level visual polish with slate color palette, refined typography, and increased whitespace.
</objective>

<execution_context>
@/Users/emanuelpereyra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emanuelpereyra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emanuelpereyra/gitRepos/own/personal-site/.planning/PROJECT.md
@/Users/emanuelpereyra/gitRepos/own/personal-site/.planning/ROADMAP.md
@/Users/emanuelpereyra/gitRepos/own/personal-site/.planning/STATE.md
@/Users/emanuelpereyra/gitRepos/own/personal-site/.planning/phases/09-accessibility-quality/09-CONTEXT.md
@/Users/emanuelpereyra/gitRepos/own/personal-site/.planning/phases/09-accessibility-quality/09-01-SUMMARY.md

<!-- Current implementation -->
@/Users/emanuelpereyra/gitRepos/own/personal-site/tailwind.config.ts
@/Users/emanuelpereyra/gitRepos/own/personal-site/src/app/globals.css
@/Users/emanuelpereyra/gitRepos/own/personal-site/src/components/Section.tsx
@/Users/emanuelpereyra/gitRepos/own/personal-site/src/app/[lang]/page.tsx
@/Users/emanuelpereyra/gitRepos/own/personal-site/src/components/Header.tsx
</context>

<interfaces>
<!-- Key patterns from codebase -->

From tailwind.config.ts:
```typescript
module.exports = {
  darkMode: 'class',
  content: ['./pages/**/*.{js,ts,jsx,tsx,mdx}', ...],
  theme: {
    extend: {},  // <-- Add slate color customization here if needed
  },
  plugins: [],
};
```

From src/components/Section.tsx:
```typescript
export default function Section({ id, children, alternate = false }: SectionProps) {
  return (
    <section
      id={id}
      className={`px-4 py-12 sm:px-8 lg:px-12 lg:py-16 ${
        alternate ? 'bg-gray-50 dark:bg-gray-900' : 'bg-white dark:bg-gray-950'
      }`}
    >
```

Current color usage patterns:
- Text: `text-gray-900 dark:text-white`, `text-gray-600 dark:text-gray-300`
- Backgrounds: `bg-white dark:bg-gray-950`, `bg-gray-50 dark:bg-gray-900`
- Borders: `border-gray-200 dark:border-gray-800`
- Hovers: `hover:bg-gray-100 dark:hover:bg-gray-800`

Current typography scale (from page.tsx):
- H1: `text-2xl sm:text-3xl lg:text-4xl font-bold`
- H2: `text-xl sm:text-2xl lg:text-3xl font-bold`
- Body: `text-lg sm:text-xl`, `text-base sm:text-lg`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Shift color palette from neutral gray to cooler slate tones</name>
  <files>tailwind.config.ts, src/components/Section.tsx, src/app/[lang]/layout.tsx, src/app/[lang]/page.tsx, src/components/Header.tsx, src/components/ThemeToggle.tsx, src/components/LanguageSwitcher.tsx, src/components/MobileDrawer.tsx</files>
  <action>
Per D-07: Replace neutral gray-* scale with cooler slate-* scale for modern, tech-forward feel (Vercel reference).

Strategy: Global find-and-replace gray- → slate- in Tailwind classes, except for skill category badges (blue/green/purple/orange) which stay as-is.

Files to update:
1. **tailwind.config.ts**: No changes needed (slate-* is built into Tailwind, no custom config required)

2. **src/components/Section.tsx**: Update alternate backgrounds
   - `bg-gray-50 dark:bg-gray-900` → `bg-slate-50 dark:bg-slate-900`
   - `bg-white dark:bg-gray-950` → `bg-white dark:bg-slate-950`

3. **src/app/[lang]/layout.tsx**: Update body background
   - `bg-white ... dark:bg-gray-950 dark:text-gray-100` → `bg-white ... dark:bg-slate-950 dark:text-slate-100`
   - Keep `text-gray-900` for light mode (will be refined in typography task)

4. **src/app/[lang]/page.tsx**: Replace all gray-* → slate-* except skill badges
   - Text colors: `text-gray-900`, `text-gray-600`, `text-gray-500`, `text-gray-300`, `text-gray-400`, `text-gray-700`
   - Backgrounds: Any gray backgrounds
   - EXCLUDE: Skill badge colors (blue-100, green-100, purple-100, orange-100 remain)
   - EXCLUDE: Portfolio tech badge colors if they use neutral gray (verify first)

5. **src/components/Header.tsx**: Update all gray classes
   - Text: `text-gray-900 dark:text-white`, `text-gray-600 ... dark:text-gray-400`
   - Backgrounds: `hover:bg-gray-100 ... dark:hover:bg-gray-800`
   - Borders: `border-gray-200 dark:border-gray-800`
   - Backdrop: `dark:bg-gray-950/80`

6. **src/components/ThemeToggle.tsx**, **LanguageSwitcher.tsx**: Update hover states and text colors (use Grep to find files with gray- classes)

7. **src/components/MobileDrawer.tsx**: Update drawer background and text colors

Implementation approach:
- Use global find in VS Code or grep to identify all `gray-` occurrences
- Replace with `slate-` equivalent number (gray-50 → slate-50, gray-900 → slate-900)
- Preserve skill category colors (blue, green, purple, orange)
- Test in both light and dark modes to verify visual consistency

Why slate: Cooler blue undertone creates modern, tech-forward feel like Vercel/Stripe, more sophisticated than neutral gray.
  </action>
  <verify>
    <automated>
# Verify build succeeds with new color classes
npm run build

# Visual check: Compare light and dark modes for cooler tone
npm run dev
# Manual: Toggle between themes, verify slate tones are cooler/bluer than before (especially noticeable in dark mode)
    </automated>
  </verify>
  <done>All components use slate-* color scale instead of gray-*, site displays cooler modern palette in both themes, skill category colors preserved</done>
</task>

<task type="auto">
  <name>Task 2: Refine typography with adjusted weights, line heights, and letter spacing</name>
  <files>src/app/globals.css, src/app/[lang]/page.tsx, src/components/Header.tsx</files>
  <action>
Per D-05: Keep text sizes (no changes to text-xl/2xl/3xl), refine font weights, line heights, and letter spacing for professional polish.

Current state: Typography works but lacks refinement. Headings feel dense, body text could breathe more.

Refinements (Claude's discretion within D-05 guideline):

1. **src/app/globals.css**: Add global typography improvements after theme rules:
```css
/* Typography refinement (D-05) */
body {
  line-height: 1.6; /* More comfortable reading */
  letter-spacing: -0.01em; /* Subtle tightening for modern feel */
}

h1, h2, h3, h4, h5, h6 {
  line-height: 1.2; /* Tighter for headings */
  letter-spacing: -0.02em; /* More pronounced for display text */
  font-weight: 700; /* Ensure bold headings */
}

/* Improve readability for body text */
p {
  line-height: 1.7;
}
```

2. **src/app/[lang]/page.tsx**: Refine specific typography instances
   - About section:
     - H1 "Emanuel Pereyra": Keep `font-bold`, add `tracking-tight` (already has it, verify)
     - Subheading "Senior Software Engineer": Change `font-medium` → `font-semibold` for more presence
     - Bio text: Add `leading-relaxed` for better readability

   - Section headings (Experience, Skills, Portfolio, Contact):
     - Keep sizes, add `tracking-tight` to all H2s for polish

   - Skills section:
     - Category headings (h3): Change `font-semibold` → `font-bold` for hierarchy
     - Tier labels: Keep `font-medium`, add `tracking-wide` for small caps feel

   - Contact section:
     - Keep heading sizes, ensure `tracking-tight` on H2

3. **src/components/Header.tsx**: Navigation typography
   - Nav links: Keep `font-medium`, add `tracking-tight` for cleaner look
   - Logo "EP": Keep `font-bold`, add `tracking-tight`

Rationale: Professional sites like Stripe use tight tracking on headings (-0.02em to -0.04em) and relaxed body text. This creates clear hierarchy and modern polish without changing sizes.
  </action>
  <verify>
    <automated>
# Build and visual check
npm run build && npm run dev
# Manual: Check typography feels more polished, headings have better hierarchy, body text is more readable
    </automated>
  </verify>
  <done>Typography displays refined weights, line heights, and letter spacing; clear hierarchy between headings and body text; professional modern feel</done>
</task>

<task type="auto">
  <name>Task 3: Increase whitespace with generous section padding and element gaps</name>
  <files>src/components/Section.tsx, src/app/[lang]/page.tsx</files>
  <action>
Per D-06: Increase whitespace for premium, less cramped feel. More generous section padding and larger gaps between elements.

Current state:
- Section padding: `py-12 lg:py-16` (48px mobile, 64px desktop)
- Element gaps: Varies (mb-4, mb-8, gap-8, gap-12)

Increases (Claude's discretion within D-06 guideline):

1. **src/components/Section.tsx**: Increase section vertical padding
   - Current: `py-12 sm:px-8 lg:px-12 lg:py-16`
   - New: `py-16 sm:px-8 lg:px-12 lg:py-24`
   - Rationale: 64px mobile → 96px desktop (50% increase) creates Stripe-like generous breathing room

2. **src/app/[lang]/page.tsx**: Increase gaps between elements

   About section:
   - Grid gap: `gap-8 lg:gap-12` → `gap-12 lg:gap-16` (more separation between image and bio)
   - Bio spacing: `mb-2` (heading to subheading) → `mb-3`
   - Bio spacing: `mb-4` (subheading to bio) → `mb-6`
   - Bio spacing: `mt-2` (between paragraphs) → `mt-4`

   Experience/Skills/Portfolio sections:
   - Heading margin: `mb-8` → `mb-12` (more space before content)

   Skills section:
   - Category spacing: `space-y-10` → `space-y-12` (more space between categories)
   - Tier spacing: `space-y-3` → `space-y-4`
   - Tier label margin: `mb-2` → `mb-3`
   - Badge gap: `gap-2` → `gap-3`

   Contact section:
   - CTA margin: `mb-8` → `mb-12`
   - Email margin: `mb-6` → `mb-8`
   - Social links gap: `gap-4` → `gap-6`

Rationale: Premium sites use 1.5x to 2x typical spacing. This creates "room to breathe" without feeling sparse. Stripe's homepage uses very generous padding (80-120px sections).
  </action>
  <verify>
    <automated>
# Build and check layout
npm run build && npm run dev
# Manual: Scroll through page, verify spacing feels more generous and premium, sections have clear separation
    </automated>
  </verify>
  <done>All sections have increased padding, elements have more generous spacing, site feels less cramped and more premium</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Color palette**: Toggle between light and dark modes, verify:
   - Slate tones are noticeably cooler (subtle blue tint) vs neutral gray
   - Dark mode backgrounds have cooler feel (slate-950, slate-900)
   - Light mode alternate sections use subtle slate-50
   - Skill badges still use original category colors (blue, green, purple, orange)

2. **Typography**: Check across all sections:
   - Headings have tighter letter spacing (tracking-tight)
   - Body text has comfortable line height (leading-relaxed)
   - Clear hierarchy: H1 > H2 > H3 > body
   - Professional polish without drastic size changes

3. **Whitespace**: Scroll through entire page:
   - Section padding noticeably larger (especially desktop)
   - More space between heading and content
   - Skills categories have clear separation
   - Contact section feels spacious, not cramped
   - Overall feel: Premium, not cluttered

Reference check: Compare to stripe.com or vercel.com for aesthetic alignment (clean, minimal, generous whitespace, cooler tones).
</verification>

<success_criteria>
- Site displays modern corporate aesthetic with Stripe/Vercel-level polish (QUAL-01)
- Typography is clear, readable, and professionally refined (QUAL-02)
- Color palette uses cooler slate tones throughout
- Generous whitespace creates premium, uncluttered feel
- Visual hierarchy is clear and professional
</success_criteria>

<output>
After completion, create `.planning/phases/09-accessibility-quality/09-02-SUMMARY.md`
</output>
