---
phase: 01-project-setup-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - src/app/globals.css
  - src/app/[lang]/layout.tsx
  - src/app/[lang]/page.tsx
  - src/middleware.ts
  - src/i18n/request.ts
  - src/i18n/routing.ts
  - src/lib/utils.ts
  - .prettierrc
  - .prettierignore
  - .husky/pre-commit
  - .lintstagedrc.js
  - messages/en.json
  - messages/es.json
  - public/placeholder.svg
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "Developer can run npm run dev and see Next.js app at localhost:3000"
    - "Images use Next.js Image component by default (no raw img tags)"
    - "Fonts are loaded via next/font without external requests"
    - "TypeScript compiles without errors in strict mode"
    - "Tailwind CSS utilities are available and purge is configured"
    - "i18n routing redirects bare / to /en"
  artifacts:
    - path: "src/app/[lang]/layout.tsx"
      provides: "Root layout with next/font Inter and Roboto Mono"
      contains: "next/font/google"
    - path: "src/app/[lang]/page.tsx"
      provides: "Home page with next/image usage"
      contains: "next/image"
    - path: "src/middleware.ts"
      provides: "Locale detection and redirect middleware"
      contains: "middleware"
    - path: "next.config.ts"
      provides: "Next.js config with next-intl plugin"
      contains: "createNextIntlPlugin"
    - path: "package.json"
      provides: "All dependencies and scripts"
      contains: "next-intl"
  key_links:
    - from: "src/middleware.ts"
      to: "src/i18n/routing.ts"
      via: "import routing config"
      pattern: "import.*routing"
    - from: "src/app/[lang]/layout.tsx"
      to: "next/font/google"
      via: "font loading"
      pattern: "Inter|Roboto_Mono"
    - from: "src/app/[lang]/page.tsx"
      to: "next/image"
      via: "image component import"
      pattern: "import Image from"
---

<objective>
Initialize the Next.js 16 project with TypeScript, Tailwind CSS 4, next-intl i18n routing, optimized fonts via next/font, optimized images via next/image, and development tooling (ESLint, Prettier, Husky, lint-staged, bundle analyzer).

Purpose: Establish the complete development foundation so all subsequent phases build on a properly configured, performant infrastructure. Addresses PERF-01 (image optimization) and PERF-02 (font optimization).

Output: A running Next.js application at localhost:3000 with i18n routing (`/en`, `/es`), optimized fonts, image patterns, and enforced code quality tooling.
</objective>

<execution_context>
@/Users/emanuelpereyra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emanuelpereyra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-project-setup-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with all dependencies and dev tooling</name>
  <files>
    package.json, tsconfig.json, next.config.ts, .prettierrc, .prettierignore,
    .husky/pre-commit, .lintstagedrc.js, postcss.config.mjs
  </files>
  <read_first>
    .planning/research/STACK.md
    .planning/phases/01-project-setup-foundation/01-CONTEXT.md
  </read_first>
  <action>
1. Create the Next.js project in the current directory (which is the git repo root). Since the repo already has a .git and .planning directory, scaffold the Next.js app into the current directory. Run:
   ```
   npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*" --turbopack --yes
   ```
   If create-next-app refuses to run in a non-empty directory, initialize in a temp directory and move files:
   ```
   npx create-next-app@latest temp-site --typescript --tailwind --app --src-dir --import-alias "@/*" --turbopack --yes
   ```
   Then move all files (except .git and .planning) from temp-site/ to the project root, and delete temp-site/.

2. Install production dependencies:
   ```
   npm install next-intl clsx tailwind-merge lucide-react
   ```

3. Install dev dependencies:
   ```
   npm install -D prettier prettier-plugin-tailwindcss eslint-config-prettier @next/bundle-analyzer husky lint-staged
   ```

4. Configure Prettier by creating `.prettierrc` with:
   ```json
   {
     "semi": true,
     "singleQuote": true,
     "tabWidth": 2,
     "trailingComma": "es5",
     "plugins": ["prettier-plugin-tailwindcss"]
   }
   ```

5. Create `.prettierignore` with:
   ```
   .next
   node_modules
   dist
   .planning
   ```

6. Configure lint-staged by creating `.lintstagedrc.js` with:
   ```js
   module.exports = {
     '*.{js,jsx,ts,tsx}': ['eslint --fix', 'prettier --write'],
     '*.{json,md,css}': ['prettier --write'],
   };
   ```

7. Initialize Husky and add pre-commit hook:
   ```
   npx husky init
   ```
   Then write `.husky/pre-commit` with content:
   ```sh
   npx lint-staged
   ```

8. Update `next.config.ts` to integrate next-intl plugin and bundle analyzer:
   ```ts
   import createNextIntlPlugin from 'next-intl/plugin';
   import type { NextConfig } from 'next';

   const withNextIntl = createNextIntlPlugin();

   const nextConfig: NextConfig = {};

   export default withNextIntl(nextConfig);
   ```

9. Add bundle analyzer script to `package.json` scripts section:
   ```json
   "analyze": "ANALYZE=true next build"
   ```

10. Verify TypeScript strict mode is enabled in `tsconfig.json` — the default create-next-app should have `"strict": true`. If not, add it under compilerOptions.

11. Verify path aliases are configured in `tsconfig.json`:
    ```json
    "paths": {
      "@/*": ["./src/*"]
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/emanuelpereyra/gitRepos/own/personal-site && node -e "const pkg = require('./package.json'); const deps = {...pkg.dependencies, ...pkg.devDependencies}; const required = ['next','react','typescript','tailwindcss','next-intl','clsx','tailwind-merge','lucide-react','prettier','prettier-plugin-tailwindcss','eslint-config-prettier','@next/bundle-analyzer','husky','lint-staged']; const missing = required.filter(r => !deps[r]); if(missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('All deps present');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next-intl" in dependencies
    - package.json contains "husky" in devDependencies
    - package.json contains "lint-staged" in devDependencies
    - package.json contains "prettier-plugin-tailwindcss" in devDependencies
    - package.json contains "@next/bundle-analyzer" in devDependencies
    - package.json scripts contains "analyze"
    - tsconfig.json contains "strict": true
    - tsconfig.json contains "@/*"
    - .prettierrc contains "prettier-plugin-tailwindcss"
    - .husky/pre-commit contains "lint-staged"
    - .lintstagedrc.js contains "eslint --fix"
    - next.config.ts contains "createNextIntlPlugin"
  </acceptance_criteria>
  <done>All dependencies installed, dev tooling configured (Prettier, ESLint, Husky, lint-staged, bundle analyzer), next.config.ts integrates next-intl plugin, TypeScript strict mode enabled, path aliases working.</done>
</task>

<task type="auto">
  <name>Task 2: Configure i18n routing, optimized fonts, image patterns, and app structure</name>
  <files>
    src/middleware.ts, src/i18n/request.ts, src/i18n/routing.ts,
    src/app/[lang]/layout.tsx, src/app/[lang]/page.tsx, src/app/globals.css,
    src/lib/utils.ts, messages/en.json, messages/es.json, public/placeholder.svg
  </files>
  <read_first>
    .planning/research/PITFALLS.md
    .planning/research/ARCHITECTURE.md
    .planning/phases/01-project-setup-foundation/01-CONTEXT.md
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    next.config.ts
  </read_first>
  <action>
1. Create i18n routing config at `src/i18n/routing.ts`:
   ```ts
   import { defineRouting } from 'next-intl/routing';

   export const routing = defineRouting({
     locales: ['en', 'es'],
     defaultLocale: 'en',
   });
   ```

2. Create i18n request config at `src/i18n/request.ts`:
   ```ts
   import { getRequestConfig } from 'next-intl/server';
   import { routing } from './routing';

   export default getRequestConfig(async ({ requestLocale }) => {
     let locale = await requestLocale;

     if (!locale || !routing.locales.includes(locale as any)) {
       locale = routing.defaultLocale;
     }

     return {
       locale,
       messages: (await import(`../../messages/${locale}.json`)).default,
     };
   });
   ```

3. Create middleware at `src/middleware.ts`:
   ```ts
   import createMiddleware from 'next-intl/middleware';
   import { routing } from './i18n/routing';

   export default createMiddleware(routing);

   export const config = {
     matcher: ['/', '/(en|es)/:path*'],
   };
   ```

4. Create message files. `messages/en.json`:
   ```json
   {
     "metadata": {
       "title": "Emanuel Pereyra - Senior Software Engineer",
       "description": "19+ years building scalable systems. Full-stack engineer specializing in modern web technologies."
     },
     "home": {
       "heading": "Senior Software Engineer",
       "subheading": "19+ years of experience building scalable, high-quality software"
     }
   }
   ```

   `messages/es.json`:
   ```json
   {
     "metadata": {
       "title": "Emanuel Pereyra - Ingeniero de Software Senior",
       "description": "19+ anios construyendo sistemas escalables. Ingeniero full-stack especializado en tecnologias web modernas."
     },
     "home": {
       "heading": "Ingeniero de Software Senior",
       "subheading": "19+ anios de experiencia construyendo software escalable y de alta calidad"
     }
   }
   ```

5. Move the existing `src/app/layout.tsx` and `src/app/page.tsx` to `src/app/[lang]/layout.tsx` and `src/app/[lang]/page.tsx`. Delete the old files. Remove any leftover `src/app/layout.tsx` and `src/app/page.tsx` (the default ones from create-next-app).

6. Rewrite `src/app/[lang]/layout.tsx` with next/font optimization (PERF-02):
   ```tsx
   import type { Metadata } from 'next';
   import { Inter, Roboto_Mono } from 'next/font/google';
   import { NextIntlClientProvider } from 'next-intl';
   import { getMessages } from 'next-intl/server';
   import { notFound } from 'next/navigation';
   import { routing } from '@/i18n/routing';
   import '../globals.css';

   const inter = Inter({
     subsets: ['latin'],
     display: 'swap',
     variable: '--font-inter',
   });

   const robotoMono = Roboto_Mono({
     subsets: ['latin'],
     display: 'swap',
     variable: '--font-roboto-mono',
   });

   export const metadata: Metadata = {
     title: 'Emanuel Pereyra - Senior Software Engineer',
     description: '19+ years building scalable systems.',
   };

   type Props = {
     children: React.ReactNode;
     params: Promise<{ lang: string }>;
   };

   export function generateStaticParams() {
     return routing.locales.map((locale) => ({ lang: locale }));
   }

   export default async function RootLayout({ children, params }: Props) {
     const { lang } = await params;

     if (!routing.locales.includes(lang as any)) {
       notFound();
     }

     const messages = await getMessages();

     return (
       <html lang={lang} className={`${inter.variable} ${robotoMono.variable}`} suppressHydrationMismatch>
         <body className="font-sans antialiased">
           <NextIntlClientProvider messages={messages}>
             {children}
           </NextIntlClientProvider>
         </body>
       </html>
     );
   }
   ```

7. Rewrite `src/app/[lang]/page.tsx` with next/image usage (PERF-01) and next-intl:
   ```tsx
   import Image from 'next/image';
   import { useTranslations } from 'next-intl';

   export default function Home() {
     const t = useTranslations('home');

     return (
       <main className="flex min-h-screen flex-col items-center justify-center p-24">
         <div className="text-center">
           <Image
             src="/placeholder.svg"
             alt="Emanuel Pereyra"
             width={120}
             height={120}
             priority={true}
             className="mx-auto mb-8 rounded-full"
           />
           <h1 className="mb-4 text-4xl font-bold tracking-tight text-gray-900 dark:text-white">
             Emanuel Pereyra
           </h1>
           <p className="text-xl text-gray-600 dark:text-gray-300">
             {t('heading')}
           </p>
           <p className="mt-2 text-lg text-gray-500 dark:text-gray-400">
             {t('subheading')}
           </p>
         </div>
       </main>
     );
   }
   ```

8. Create a placeholder SVG at `public/placeholder.svg`:
   ```svg
   <svg xmlns="http://www.w3.org/2000/svg" width="120" height="120" viewBox="0 0 120 120">
     <rect width="120" height="120" rx="60" fill="#e5e7eb"/>
     <text x="50%" y="50%" dominant-baseline="central" text-anchor="middle" font-size="40" fill="#9ca3af">EP</text>
   </svg>
   ```

9. Update `src/app/globals.css` to use Tailwind v4 import syntax and set font-family:
   ```css
   @import 'tailwindcss';

   @theme {
     --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
     --font-mono: var(--font-roboto-mono), ui-monospace, monospace;
   }
   ```

10. Create utility file at `src/lib/utils.ts`:
    ```ts
    import { clsx, type ClassValue } from 'clsx';
    import { twMerge } from 'tailwind-merge';

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs));
    }
    ```

11. Ensure there is NO `src/app/layout.tsx` or `src/app/page.tsx` at the root app level (only under `[lang]/`). If create-next-app left them, delete them.

12. Run `npm run build` to verify TypeScript compiles and the build succeeds.

13. Verify no external font requests by checking the build output does not reference fonts.googleapis.com.
  </action>
  <verify>
    <automated>cd /Users/emanuelpereyra/gitRepos/own/personal-site && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/[lang]/layout.tsx contains "Inter" from "next/font/google"
    - src/app/[lang]/layout.tsx contains "Roboto_Mono" from "next/font/google"
    - src/app/[lang]/layout.tsx contains "display: 'swap'"
    - src/app/[lang]/layout.tsx contains "--font-inter"
    - src/app/[lang]/layout.tsx contains "NextIntlClientProvider"
    - src/app/[lang]/page.tsx contains "import Image from 'next/image'"
    - src/app/[lang]/page.tsx contains "useTranslations"
    - src/middleware.ts contains "createMiddleware"
    - src/middleware.ts contains "routing"
    - src/i18n/routing.ts contains "locales: ['en', 'es']"
    - src/i18n/request.ts contains "getRequestConfig"
    - messages/en.json contains "Senior Software Engineer"
    - messages/es.json contains "Ingeniero de Software Senior"
    - src/app/globals.css contains "@import 'tailwindcss'"
    - src/app/globals.css contains "--font-inter"
    - src/lib/utils.ts contains "twMerge"
    - NO file src/app/layout.tsx exists (only src/app/[lang]/layout.tsx)
    - NO file src/app/page.tsx exists (only src/app/[lang]/page.tsx)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>i18n routing works (/ redirects to /en), fonts load via next/font with no external requests (PERF-02), page uses next/image component (PERF-01), Tailwind CSS utilities render correctly, TypeScript compiles without errors, build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` serves app at localhost:3000
3. Visiting `/` redirects to `/en`
4. Visiting `/es` shows Spanish content
5. No `fonts.googleapis.com` requests in network tab (fonts self-hosted via next/font)
6. Page uses `<Image>` component (no raw `<img>` tags in source)
7. `npx tsc --noEmit` passes (TypeScript strict mode, no errors)
8. `npx prettier --check "src/**/*.{ts,tsx}"` passes
9. `npx eslint src/` passes
</verification>

<success_criteria>
- Developer runs `npm run dev` and sees the portfolio landing page at localhost:3000
- Images are rendered via Next.js Image component (PERF-01)
- Fonts load via next/font with no external Google Fonts requests (PERF-02)
- TypeScript compiles in strict mode without errors
- Tailwind CSS utilities work and purge is configured
- i18n routing is functional: / redirects to /en, /es shows Spanish
- Pre-commit hooks run ESLint + Prettier via Husky + lint-staged
- Bundle analyzer is available via `npm run analyze`
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-foundation/01-01-SUMMARY.md`
</output>
